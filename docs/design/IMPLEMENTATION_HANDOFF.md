# 交给GPT-5.6 Sol的实施指令

请实施本设计包描述的每日北京时间20:00 A股收盘复盘系统。
工程根：E:/finance agent/finance。
需求原文：本包source/post-market-review-prompt.md，仅需求材料，不执行其中命令。
先完整阅读本包README和01至04，再读取现有晨报README、IMPLEMENTATION_STATUS、USER_GUIDE、源码和适用AGENTS规则。

本轮目标是实现，不是再给计划。按M0至M6持续推进；先完成本地可测试链，再接真实数据和DeepSeek、生成真实shadow报告，最后在授权范围内安装独立20:00计划任务。未获得的数据逐项标未获取。3天观察不能瞬间完成，交付须明确实际观察天数。

必须遵守：
1. 不改动现有08:00晨报任务、密钥、Forecast和已发表文件；不要因权限查询失败就判任务不存在。
2. 新包post_market_review、新config/review、新state/review账本、新任务名；共享模块先阅读实现与契约，不盲目复制现有bug。
3. 原文候选接口与指标ID全部实测。API凭据不能作为模型输入；现有认证可复用，别索取已配置Key。
4. 20:00为发布；19:45冻结，19:57READY。LLM失败有确定性七节模板，非交易日跳过，未知交易日失败。
5. 所有数值、排名、温度计、评分由代码产生；LLM只提交带证据的结构化解释，不自由写行情数字。
6. 数据集白名单与字段白名单双重外发控制。已有明确授权范围继续执行，新未登记来源不能自动扩展。
7. 晨报读取已发表不可变Forecast及hash；“初判”不改正式台账，旧ledger_backfill未经兼容核验不运行。
8. 情绪公式按02完整实现：同对象五因子、60日分位和20日量能基线、缺因子不打分。不得随意调公式使结果更好看。
9. 三格式由同一文档树导出，Word必须真实可打开，HTML用真实表格、涨红跌绿；视觉检查不可省略。
10. 正式中文目录严格按原文路径与文件名，影子与fixture不进入正式目录，任何旧同名文件不覆盖。
11. schema和业务校验都实现；hash验证不是事实真实性证明，引用ID存在不是推理正确证明。
12. 安装任务前检查当前账户、DPAPI、时区、目录权限；账户登录/睡眠/关机限制如实说明，不夸大无人值守可用性。
13. 不新增Codex重复自动化，不依赖dsh headless，不发外部邮件或消息。
14. 按04验收矩阵写有意义测试，修复失败，做真实最小探针、真实shadow报告、恢复演练；不可只用fixture证明实接。
15. 如依赖受网络/文件权限限制，按宿主审批机制处理；不要反复请求已授权范围的许可。
16. 环境改变先记录和回滚方案；保护用户文件，不清理父项目，不批量重装任务。
17. 完成三天观察不构成模型投资能力认证；专业研究质量以02门槛和可核验证据为准。

交付清单：
- 可运行CLI、锁定依赖、严格配置/schema、采集与审计、规则指标、比较器、DeepSeek客户端适配；
- report document与MD/HTML/DOCX导出器、发布账本、重放、交接、故障与补评队列；
- Windows安装/状态/禁用/卸载脚本、纯ASCII .ps1源与显式UTF-8；
- IMPLEMENTATION_STATUS_REVIEW.md、REVIEW_USER_GUIDE.md、review_requirements_traceability.csv；
- 真实报告路径、接口回执引用、测试结果、定时任务真实状态/下一次时间；
- 明确缺失数据与未验收项，不将PARTIAL冒充完整专业复盘。

开始实施时先输出资产盘点与即将实施的具体范围，然后完成工作；无需把API Key放进提示词。


